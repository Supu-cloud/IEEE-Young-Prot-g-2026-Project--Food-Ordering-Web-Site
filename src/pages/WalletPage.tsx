import { CreditCard, Plus, ShieldCheck, WalletCards } from 'lucide-react'
import { useState } from 'react'
import { formatLkr } from '../services/sriLankanData'
export function WalletPage() { const [open,setOpen]=useState(false); return <div className="page container"><div className="page-title"><span className="eyebrow">Payments</span><h1>Foodie Wallet</h1><p>Choose how you pay for your favourite local meals.</p></div><div className="wallet-layout"><section className="wallet-balance"><WalletCards /><span>Available balance</span><strong>{formatLkr(5000)}</strong><small>Wallet top-ups are a UI preview until a backend wallet endpoint is available.</small></section><section className="account-section"><div className="section-heading"><h2>Payment methods</h2><button className="button button--primary" onClick={()=>setOpen(true)}><Plus /> Add method</button></div><div className="payment-option-list"><article><span><strong>Cash on delivery</strong><small>Pay the rider when your order arrives</small></span><b>Available</b></article><article><CreditCard /><span><strong>Visa ending in 4242</strong><small>Saved-card placeholder · no charge will be made</small></span><button>Remove</button></article><article><WalletCards /><span><strong>Foodie Wallet</strong><small>Use your available wallet balance</small></span><button>Default</button></article></div><div className="secure-note"><ShieldCheck /> Online payment is confirmed only after the backend creates a valid payment intent.</div></section></div>{open&&<div className="modal-backdrop" onClick={()=>setOpen(false)}><form className="portal-modal" onClick={(e)=>e.stopPropagation()} onSubmit={(e)=>{e.preventDefault();setOpen(false)}}><h2>Add payment method</h2><label>Cardholder name<input required /></label><label>Card number<input required inputMode="numeric" placeholder="0000 0000 0000 0000" /></label><div className="form-grid"><label>Expiry<input required placeholder="MM / YY" /></label><label>CVV<input required placeholder="123" /></label></div><p>This interaction stores no card details. A secure payment-provider integration is required.</p><button className="button button--primary button--full">Save placeholder</button></form></div>}</div> }
