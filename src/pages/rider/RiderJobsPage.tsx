import { Clock3, DollarSign, MapPin, Navigation, Phone, Store, X } from 'lucide-react'
import { Link } from 'react-router-dom'
import { deliveryJobs } from '../../services/portalService'
export function RiderJobsPage(){const jobs=deliveryJobs.filter((j)=>j.status==='Available');return <><div className="portal-title"><div><span className="eyebrow">Live opportunities</span><h1>Delivery requests</h1><p>Review pickup, drop-off, distance, and payout before accepting.</p></div><div className="live-indicator"><span/> Searching nearby</div></div><div className="rider-job-list">{jobs.map((job)=><article key={job.id}><div className="job-main"><div className="job-payout"><span>Payout</span><strong>${job.payout.toFixed(2)}</strong></div><div className="route-detail"><div><Store/><span><small>Pick up from</small><strong>{job.restaurant}</strong><em>{job.pickup}</em></span></div><i/><div><MapPin/><span><small>Deliver to</small><strong>{job.distance}</strong><em>{job.dropoff}</em></span></div></div><div className="job-meta"><span><Clock3/> 24–32 min</span><span><Navigation/> {job.distance}</span><span><DollarSign/> ${job.payout.toFixed(2)} guaranteed</span></div></div><div className="job-actions"><button><X/> Decline</button><Link className="button button--primary" to={`/rider/jobs/${job.id}`}>Review & accept</Link></div></article>)}</div><div className="portal-card rider-safety"><Phone/><div><h3>Stay safe while riding</h3><p>Only accept requests when parked safely. Never interact with the app while moving.</p></div></div></>}
