import { Bike, CheckCircle2, Eye, EyeOff, Store, UserRound, Utensils } from 'lucide-react'
import { useState, type FormEvent } from 'react'
import { Link, useNavigate, useSearchParams } from 'react-router-dom'
import { AppApiError } from '../core/api/apiError'
import { roleHome, useAuth } from '../core/auth/AuthContext'
import type { UserRole } from '../core/types/api'

const roleInfo = {
  customer: { label: 'Customer', icon: UserRound, description: 'Order local favourites and track deliveries.' },
  restaurant_owner: { label: 'Restaurant owner', icon: Store, description: 'Manage your restaurant, menu and orders.' },
  delivery_rider: { label: 'Delivery rider', icon: Bike, description: 'Deliver orders and track your earnings.' },
} as const

export function AuthV2Page({ mode }: { mode: 'login' | 'register' }) {
  const [params] = useSearchParams(); const navigate = useNavigate(); const auth = useAuth(); const queryRole = params.get('role') as UserRole | null
  const [role, setRole] = useState<UserRole>(queryRole && roleInfo[queryRole] ? queryRole : 'customer'); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const [success, setSuccess] = useState(''); const [showPassword, setShowPassword] = useState(false)
  const selected = roleInfo[role]; const SelectedIcon = selected.icon
  const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setBusy(true); setError(''); const form = new FormData(event.currentTarget); const password = String(form.get('password') ?? ''); if (password.length < 6) { setError('Password must contain at least 6 characters.'); setBusy(false); return } try { if (mode === 'login') { const user = await auth.login(String(form.get('email')), password, form.get('remember') === 'on'); navigate(roleHome[user.role], { replace: true }) } else { const confirm = String(form.get('confirmPassword')); if (password !== confirm) throw new Error('Passwords do not match.'); const message = await auth.register(role, { name: String(form.get('name')), email: String(form.get('email')), password, phone: String(form.get('phone')), address: String(form.get('address')) }); setSuccess(role === 'customer' ? `${message}. You can now sign in.` : `${message}. Your application is pending administrator approval. You can sign in only after it is approved.`) } } catch (caught) { const message = caught instanceof AppApiError || caught instanceof Error ? caught.message : 'Something went wrong.'; setError(message.startsWith('Account is ') ? `${message}. Restaurant-owner and rider accounts can sign in only after administrator approval.` : message) } finally { setBusy(false) } }
  if (success) return <div className="auth-page"><div className="application-success"><span><CheckCircle2 /></span><h1>{role === 'customer' ? 'Ayubowan to Foodie!' : 'Application received'}</h1><p>{success}</p><Link className="button button--primary" to="/login">Continue to sign in</Link><Link to="/">Return to Foodie</Link></div></div>
  return <div className="auth-page"><Link className="auth-brand" to="/"><span><Utensils /></span> Foodie</Link><div className="auth-shell"><section className="auth-intro"><span className="eyebrow">One platform, every role</span><h1>{mode === 'login' ? 'Ayubowan!' : 'Join Foodie.'}</h1><p>{mode === 'login' ? 'Sign in once and we’ll take you to the right workspace.' : 'Choose how you’ll use Foodie and create your account.'}</p>{mode === 'register' && <div className="role-options">{Object.entries(roleInfo).map(([key, info]) => { const Icon = info.icon; return <button className={role === key ? 'active' : ''} type="button" onClick={() => setRole(key as UserRole)} key={key}><Icon /><span><strong>{info.label}</strong><small>{info.description}</small></span>{role === key && <CheckCircle2 />}</button> })}</div>}{mode === 'login' && <div className="auth-local-copy"><strong>From Colombo to Kandy, your favourites are closer.</strong><p>One secure account for customers, restaurant partners and delivery riders.</p></div>}</section><form className="auth-form" onSubmit={submit} noValidate><span className="auth-role-icon"><SelectedIcon /></span><h2>{mode === 'login' ? 'Sign in to Foodie' : role === 'customer' ? 'Create customer account' : `${selected.label} application`}</h2>{error && <div className="form-alert" role="alert">{error}<button type="button" onClick={() => setError('')}>×</button></div>}{mode === 'register' && <label>Full name<input required name="name" autoComplete="name" placeholder="Enter your full name" /></label>}<label>Email address<input required name="email" type="email" autoComplete="email" placeholder="you@example.com" /></label>{mode === 'register' && <><label>Phone number<input required name="phone" type="tel" autoComplete="tel" placeholder="07X XXX XXXX" /></label><label>Address<textarea required name="address" autoComplete="street-address" placeholder="Your delivery or business address" /></label></>}<label>Password<div className="password-field"><input required name="password" type={showPassword ? 'text' : 'password'} minLength={6} autoComplete={mode === 'login' ? 'current-password' : 'new-password'} placeholder="At least 6 characters" /><button type="button" aria-label={showPassword ? 'Hide password' : 'Show password'} onClick={() => setShowPassword(!showPassword)}>{showPassword ? <EyeOff /> : <Eye />}</button></div></label>{mode === 'register' && <label>Confirm password<input required name="confirmPassword" type="password" minLength={6} autoComplete="new-password" placeholder="Repeat your password" /></label>}<label className="check"><input name={mode === 'login' ? 'remember' : 'terms'} type="checkbox" required={mode === 'register'} defaultChecked={mode === 'login'} /> {mode === 'login' ? 'Remember me on this device' : 'I accept the Terms and Conditions'}</label>{mode === 'register' && role !== 'customer' && <div className="pending-note">Applications are reviewed by an administrator. You cannot sign in until your account is approved.</div>}<button className="button button--primary button--full" disabled={busy}>{busy ? 'Please wait…' : mode === 'login' ? 'Sign in' : role === 'customer' ? 'Create account' : 'Submit application'}</button><p>{mode === 'login' ? 'New to Foodie?' : 'Already have an account?'} <Link to={mode === 'login' ? '/register' : '/login'}>{mode === 'login' ? 'Create an account' : 'Sign in'}</Link></p></form></div></div>
}
