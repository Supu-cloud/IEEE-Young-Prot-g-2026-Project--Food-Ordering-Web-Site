import { ArrowLeft, Candy, Search } from 'lucide-react'
import { useMemo, useState } from 'react'
import { Link } from 'react-router-dom'
import { FoodCard } from '../components/FoodCard'
import { EmptyState, ErrorState, LoadingState } from '../components/ui/AsyncState'
import { useFoodCatalogue } from '../hooks/useFoodCatalogue'
export function SweetShopPage(){const resource=useFoodCatalogue();const[query,setQuery]=useState('');const sweets=useMemo(()=>(resource.data?.dishes??[]).filter((dish)=>dish.category.toLowerCase()==='sweets'&&dish.name.toLowerCase().includes(query.toLowerCase())),[resource.data,query]);return <div className="sweet-page"><section className="sweet-hero"><div className="container"><Link to="/" className="back-link"><ArrowLeft/> Back home</Link><span><Candy/> A little taste of celebration</span><h1>Sri Lankan Sweet Shop</h1><p>Discover beloved island sweets, prepared in their authentic shapes, textures and golden colours.</p><label><Search/><input value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="Search traditional sweets"/></label></div></section><main className="container sweet-catalogue"><div className="section-heading"><div><span className="eyebrow">Handmade with tradition</span><h2>All sweets</h2><p>{sweets.length} local favourites</p></div></div>{resource.loading?<LoadingState label="Opening the sweet shop…"/>:resource.error?<ErrorState message={resource.error} retry={resource.retry}/>:sweets.length?<div className="food-grid">{sweets.map((dish)=><FoodCard dish={dish} key={dish._id}/>)}</div>:<EmptyState title="No sweets found" message="Try another search, or check back when our partners update their menus."/>}</main></div>}
