import { Bell, CreditCard, HelpCircle, LogOut, MapPin, Megaphone, Pencil, UserRound, WalletCards } from 'lucide-react'
import { useState, type FormEvent } from 'react'
import { Link } from 'react-router-dom'
import { profileApi } from '../core/api/services'
import { useAsyncResource } from '../core/api/useAsyncResource'
import { useAuth } from '../core/auth/AuthContext'
import { ErrorState, LoadingState } from '../components/ui/AsyncState'
export function AccountConnectedPage(){const auth=useAuth();const resource=useAsyncResource(profileApi.get);const[editing,setEditing]=useState(false);if(resource.loading)return <div className="page container"><LoadingState label="Loading your Foodie profile…"/></div>;if(resource.error||!resource.data)return <div className="page container"><ErrorState message={resource.error||'Profile not found'} retry={resource.retry}/></div>;const user=resource.data;const save=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();const form=new FormData(event.currentTarget);await profileApi.update({name:String(form.get('name')),phone:String(form.get('phone')),address:String(form.get('address'))});setEditing(false);await resource.retry()};return <div className="page container"><div className="account-layout"><aside className="account-nav"><a className="active" href="#profile"><UserRound/> Profile</a><a href="#address"><MapPin/> Address</a><a href="#payments"><CreditCard/> Payments</a><a href="#notifications"><Bell/> Notifications</a><a href="#offers"><Megaphone/> Offers</a><a href="#help"><HelpCircle/> Help centre</a><button onClick={auth.signOut}><LogOut/> Sign out</button></aside><div className="account-content"><section className="profile-card" id="profile"><span className="profile-initial">{user.name.charAt(0).toUpperCase()}</span><div><h1>{user.name}</h1><p>{user.email}</p><p>{user.phone||'Add your phone number'}</p></div><button className="button button--secondary" onClick={()=>setEditing(true)}><Pencil/> Edit profile</button></section><section className="account-section" id="address"><div className="section-heading"><h2>Delivery address</h2></div><article className="saved-address"><MapPin/><div><strong>Default address</strong><p>{user.address||'Add a delivery address to make checkout faster.'}</p></div></article></section><section className="account-section" id="payments"><div className="section-heading"><h2>Wallet & payments</h2><Link to="/wallet">Manage payments</Link></div><div className="payment-cards"><article><WalletCards/><span><strong>Foodie Wallet</strong><small>View balance and payment options</small></span></article><article><CreditCard/><span><strong>Cash or card</strong><small>Choose your method at checkout</small></span></article></div></section><section className="account-section" id="notifications"><h2>Notifications</h2><div className="settings-list"><label><span><strong>Order updates</strong><small>Preparation and delivery status</small></span><input type="checkbox" defaultChecked/></label><label><span><strong>Offers and promotions</strong><small>Local Foodie deals and new restaurants</small></span><input type="checkbox" defaultChecked/></label></div></section><section className="account-section" id="offers"><h2>Offers & promotions</h2><div className="coupon-row"><article><span>20% OFF</span><strong>Your first Foodie order</strong><small>Code: FOODIE20</small></article><article><span>FREE DELIVERY</span><strong>Orders above Rs. 2,000</strong><small>Code: ISLAND20</small></article></div></section><section className="account-section" id="help"><h2>Help centre</h2><p>Need help with an order, payment or account? Contact the Foodie support team.</p><button className="button button--secondary">Contact support</button></section></div></div>{editing&&<div className="modal-backdrop" onClick={()=>setEditing(false)}><form className="portal-modal" onClick={(e)=>e.stopPropagation()} onSubmit={(e)=>void save(e)}><h2>Edit personal information</h2><label>Name<input name="name" defaultValue={user.name} required/></label><label>Phone<input name="phone" defaultValue={user.phone}/></label><label>Delivery address<textarea name="address" defaultValue={user.address}/></label><button className="button button--primary button--full">Save changes</button></form></div>}</div>}
